apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: devopschallenge
  labels:
    app: postgres
type: Opaque
stringData:
  user: ((postgres-username))
  password: ((postgres-password))
  db: ((postgres-db))
